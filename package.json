{
  "name": "vzhyk",
  "private": true,
  "version": "0.1.0",
  "description": "ВЖИК — україномовна платформа оголошень (marketplace). Monorepo: api (NestJS), web (Next.js), admin (Next.js route group in web for MVP).",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/api & npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=apps/api && npm run build --workspace=apps/web",
    "lint": "npm run lint --workspace=apps/api && npm run lint --workspace=apps/web",
    "typecheck": "npm run typecheck --workspace=apps/api && npm run typecheck --workspace=apps/web",
    "test": "npm run test --workspace=apps/api && npm run test --workspace=apps/web",
    "format": "prettier --write \"apps/**/*.{ts,tsx}\"",
    "migration:run": "npm run migration:run --workspace=apps/api",
    "migration:generate": "npm run migration:generate --workspace=apps/api",
    "seed": "npm run seed --workspace=apps/api"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
